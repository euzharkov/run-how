{
  // web app
  "name": "web",
  "targets": {
    "serve": { "executor": "@nx/vite:dev-server" },
    "build": { "executor": "@nx/vite:build", "metadata": { "description": "Production bundle" } },
    "test": { "executor": "@nx/vitest:test" },
    "lint": { "executor": "@nx/eslint:lint" },
    "e2e": { "executor": "@nx/playwright:playwright" },
    "deploy": { "executor": "nx:run-commands", "options": { "command": "wrangler deploy" } },
  }
}
